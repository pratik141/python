[postgresqlReader]
host=localhost
database=postgres
user=postgres
password=postgres
port=5433

[postgresqlWriter]
host=localhost
database=postgres
user=postgres
password=postgres
port=5433


select 'da', count(*) from da_api_metrics union select 'tw', count(*) from tw_api_metrics;